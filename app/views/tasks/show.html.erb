<h2><%= @task.title%></h2>

<p><%= @task.duedate%></p>

<p><%= @task.content%></p>

<p><%= @task.location%></p>

<% 	require 'open-uri'
	require 'json'
	open('http://api.wunderground.com/api/4a9cdbbd8fedfdc6/geolookup/conditions/q/IA/Cedar_Rapids.json') do |f|
		  json_string = f.read
		  parsed_json = JSON.parse(json_string)
		  location = parsed_json['location']['city']
		  temp_f = parsed_json['current_observation']['temp_f']
		  print "Current temperature in #{location} is: #{temp_f}\n"
		   "Current temperature in #{location} is: #{temp_f}."
end %>


<%= link_to 'Edit task', edit_task_path %>

<%= link_to 'Delete task', task_path, method: :delete %>



