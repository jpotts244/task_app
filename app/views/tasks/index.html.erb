
<div class="row">  
  <div class="col-md-6 col-md-offset-3">

  	<%= render 'form_search'%>


    <h1>Tasks</h1><hr>
    <% @tasks.each do |t| %>
        <a href='/tasks/<%= t.id%>' class="tasktitle"><p><%= t.title %></a></p><br>
          
    <% end %>
  </div>
</div>

