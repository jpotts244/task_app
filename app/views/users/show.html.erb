<%= @user.name %><br>
<%= @user.email %><br>

<% @tasks.each do |task| %>
	<%= task.title %><br>
	<%= task.content %><br>
<% end %>

<%= link_to "Create New Task", new_task_path %>

<%= link_to "Mail Box", user_messages_path(@user.id) %>

<% if User.find_by(id: session[:user_id]) %>

<%= link_to "logout", logout_path, method: :delete %>

<% end %>